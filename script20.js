var kiwis = 3;
const message = `J'ai ${kiwis} kiwis dans mon panier;`;

console.log(message);

const hours = "15H35";
const phrase = ", l'heure de se coucher";

function test(hours, phrase) {
  const message = `il est ${hours}`;
  console.log(message);
}

test(hours, phrase);
